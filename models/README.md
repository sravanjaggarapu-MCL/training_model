# models/

Base (pretrained) checkpoints live here — `yolov8n.pt`, `yolov5su.pt`, etc.

You don't need to add anything manually: `train.py` configures Ultralytics to download the chosen base model into this folder automatically on first use. Contents are gitignored.
